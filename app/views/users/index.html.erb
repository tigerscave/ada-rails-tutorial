<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <span>name: <%= link_to user.name, user %></span>
      <span>, email: <%= user.email %></span>
    </li>
  <% end %>
</ul>